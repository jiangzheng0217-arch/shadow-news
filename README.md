# Shadow News

Shadow News is a lightweight AIHOT topic reader. It fetches live AIHOT public items, rewrites noisy AI headlines into plain-language topic cards, and keeps original source links visible.

## Run locally

```bash
npm install
npm start
```

Open `http://127.0.0.1:5188`.

## Deploy

The repo includes `render.yaml` for Render Web Service deployment.
